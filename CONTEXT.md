# sharp-compress-cli

sharp で画像を圧縮するコマンドラインツール。`input/` に置いた画像を、同じ構造で `out/` に圧縮して書き出す。

## Language

### 処理

**圧縮**:
入力画像を、より小さいファイルに書き出す処理の総称。再エンコード・リサイズ・トランスコードのいずれか、または組み合わせ。
_Avoid_: 最適化、変換（単独では使わない）

**再エンコード**:
フォーマットと寸法を変えずに、品質を落として書き直すこと。何も指定しないときの圧縮はこれだけを行う。
_Avoid_: 再圧縮、品質変更

**リサイズ**:
最大幅・最大高に収まるよう縮小すること。拡大はしない。
_Avoid_: 縮小、スケーリング

**トランスコード**:
別の出力フォーマットで書き出すこと。
_Avoid_: フォーマット変換、コンバート

**コピー**:
圧縮せず、入力ファイルをそのまま出力先に置くこと。圧縮すると大きくなるときと、圧縮できないフォーマットのときに行う。
_Avoid_: スキップ、素通し、パススルー

### 入出力

**入力**:
リポジトリ直下の `input/` に置いた画像。サブディレクトリも再帰的に走査する。
_Avoid_: ソース、対象、引数

**出力先**:
リポジトリ直下の `out/`。`input/` 内のディレクトリ構造をそのまま写して書き出す。
_Avoid_: 出力ディレクトリ、dist、--out-dir

**出力フォーマット**:
書き出せる画像形式。JPEG・PNG・WebP・AVIF の 4 つ。
_Avoid_: 対応フォーマット、ターゲットフォーマット
